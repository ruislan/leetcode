use crate::Solution;

impl Solution {
    pub fn reverse_bits(n: i32) -> i32 {
        // 方法1
        // 辗转相除2，余数进入栈，然后pop栈
        // 创建和num = 0
        // 记录pop的个数s，按照num += 2.pow(s) * pop()
        // 返回num
        // 这道题Rust没有输入，换成Kotlin或者Java来提交
        0
    }
}