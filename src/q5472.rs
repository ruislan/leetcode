use crate::Solution;

impl Solution {
    pub fn restore_string(s: String, indices: Vec<i32>) -> String {
        // 方法1
        // 创建res，迭代indices，按照indices[i]的索引从s中取出来逐个push到res中
        // 返回res
        "".to_string()
    }
}