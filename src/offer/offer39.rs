use crate::offer::Solution;

impl Solution {
    pub fn majority_element(nums: Vec<i32>) -> i32 {
        // 方法1
        // 创建一个50001的数组arr，然后将nums[i]放入arr
        // 迭代arr，当值大于nums.len() / 2 时，即为结果
        0
    }
}