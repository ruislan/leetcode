use crate::Solution;

impl Solution {
    pub fn replace_space(s: String) -> String {
        // 方法1
        // 利用库函数replace直接替换

        // 方法2
        // 迭代s中的char，遇到空格就替换成%20
        "".to_string()
    }
}