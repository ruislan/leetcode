use crate::Solution;

impl Solution {
    pub fn is_anagram(s: String, t: String) -> bool {
        // 方法1
        // 构建一个26长度的数组freq_s和freq_t，
        // 迭代s，将字符的频率放入freq_s
        // 迭代t，将字符的频率放入freq_t
        // 判断两个数组是否相等即可
        true
    }
}